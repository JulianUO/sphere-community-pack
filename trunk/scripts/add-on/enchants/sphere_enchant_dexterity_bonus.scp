[typedef t_enchant_dexterity_bonus]
on=@equip
if <def.scp.MagicProperties_DexterityBonus> == 1
src.moddex += <tag0.dexbonus>
endif

on=@unequip
if <def.scp.MagicProperties_DexterityBonus> == 1
src.moddex -= <tag0.dexbonus>
endif

[eof]
